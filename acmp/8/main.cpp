#include <bits/stdc++.h>

using namespace std;

int main() {
    int a;
    int b;
    int c;
    scanf("%d %d %d", &a, &b, &c);
    if (a * b == c) {
        printf("YES");
    } else {
        printf("NO");
    }
    return 0;
}