program Digits2;
const E = 0.000001;
var l, k: longint;
    A: array[1..2000] of byte;
function Next(l: real; k: longint)
begin
 if k = 1 then
  begin
   if
  end;
end;
begin
 fillchar(A, sizeof(A), 0);
 read(l, k);

 write(Next(l, k));
end.
